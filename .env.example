# Cassius - Variables d'environnement
# Copiez ce fichier vers .env et remplissez les valeurs

# Environnement (development | production)
APP_ENV=development

# URIs Supabase (copier depuis Supabase Dashboard > Settings > Database > Connection string > URI)
# IMPORTANT: Utiliser le Session pooler (port 6543) pour Replit
# IMPORTANT: Mot de passe alphanumerique uniquement (pas de @, #, %)
SUPABASE_DB_URL_DEV=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
SUPABASE_DB_URL_PROD=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

# Configuration DB (optionnel)
DB_SSL=true
DB_POOL_MAX=5
DB_CONN_TIMEOUT_MS=60000

# Secrets (generer des valeurs aleatoires securisees)
SESSION_SECRET=your-session-secret-here
JWT_SECRET=your-jwt-secret-here
